#include "Rectangle.h"
//#include "Circle.h"

/*
 *  After you implement all the classes above, write a main function 
 *  Create an array of Shape and add different Circle and Rectangle with different sizes into this array.
*/
int main(void){
    // Rectangle notQuiteSquare(10,10,2,3);
    // Shape *board[10][10];

    // // putting the shape inside of the board at the specified location
    // board[notQuiteSquare.getCenterX()][notQuiteSquare.getCenterY()] = &notQuiteSquare;

    
    // return 0;

    Circle what(10,10,5)
}